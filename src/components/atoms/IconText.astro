---
interface Props {
  icon: string;
  text: string;
}

const { icon, text } = Astro.props;
---

<div class="flex flex-col gap-4 items-center">
  <img src={icon} alt="" />
  <span class="max-w-[190px] font-semibold text-center mx-auto">{text}</span>
</div>
