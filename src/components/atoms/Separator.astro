---
interface Props {
  size?: 'sm' | 'md' | 'lg';
}
const { size = 'lg' } = Astro.props;
---

<div
  class:list={[
    'w-full',
    size === 'sm' && 'h-8 sm:h-12',
    size === 'md' && 'h-12 sm:h-20',
    size === 'lg' && 'h-20 sm:h-32',
  ]}
>
</div>
